[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vet-prescription-generator"
version = "2.0.0"
description = "Generador automatizado de recetas médicas veterinarias en DOCX y PDF."
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Klever López", email = "lkleverjosue@gmail.com" }
]
license = { text = "MIT" }
keywords = ["veterinary", "prescription", "docx", "pdf", "tkinter", "python"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501", "B008", "W291"]
